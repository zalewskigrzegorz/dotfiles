
[metadata]
name = "nu-history"
description = "A channel to select from your nu history"

[source]
command = "bash -lc 'nu \"$HOME/.config/television/cable/nu-history-source.nu\" </dev/null'"
display = "{split:\t:1..}"
output = "{split:\t:1..}"
no_sort = true
frecency = false
