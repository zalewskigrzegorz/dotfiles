# Layer screenshots

Drop a fresh Bazecor screenshot per layer here, named exactly:
- `layer-main.png`
- `layer-arrows.png`
- `layer-media.png`
- `layer-mouse.png`

They auto-render in `../LAYERS.md`. Re-shoot after layout changes — the
versioned `../defy-layout.json` is the authoritative historical backup
(full git history); these are the visual aid.
